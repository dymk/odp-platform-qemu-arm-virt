# Each non-comment line is a driver name published by odp-windows-drivers.
# The E2E runner resolves matching <name>.zip assets to immutable IDs and
# SHA-256 digests before caching or dispatching the image workflow.
driver-pl061gpio-ARM64-Release
driver-qemui2c-ARM64-Release
driver-ectest_kmdf-ARM64-Release
