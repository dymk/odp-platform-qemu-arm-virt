[package]
name = "windows-acpi-e2e-guest-support"
version = "0.1.0"
edition = "2024"
license = "MIT"
rust-version = "1.90"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Devices_DeviceAndDriverInstallation",
    "Win32_Devices_Properties",
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Storage_FileSystem",
    "Win32_System_IO",
] }

[workspace]
